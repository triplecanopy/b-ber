{
  "name": "@canopycanopycanopy/b-ber-shapes",
  "version": "0.1.0-alpha.14",
  "description": "",
  "main": "index.js",
  "directories": {
    "dc": "dc",
    "directives": "directives",
    "sequences": "sequences"
  },
  "scripts": {
    "prepare.lib": "BABEL_ENV=production ../../node_modules/.bin/babel -d ./ src/ --ignore **/__tests__/**",
    "prepare": "yarn clean && yarn prepare.lib",
    "clean": "rimraf dc && rimraf directives && rimraf sequences",
    "build": "yarn prepare",
    "watch": "BABEL_ENV=production ../../node_modules/.bin/babel -d lib/ src/ --watch src --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maxwell Simmer <maxwell.simmer@gmail.com> (http://maxwellsimmer.com)",
  "license": "MIT",
  "devDependencies": {
    "jest": "^22.4.3",
    "rimraf": "^2.6.2"
  }
}
