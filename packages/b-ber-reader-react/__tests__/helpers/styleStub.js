// Jest stand-in for side-effect style imports (`import './index.scss'`,
// `import 'material-icons/.../filled.css'`). These have no JS surface, so an
// empty module is all the test environment needs.
module.exports = {}
